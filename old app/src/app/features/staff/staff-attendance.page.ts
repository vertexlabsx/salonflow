import { DatePipe } from "@angular/common";
import { Component, OnDestroy, OnInit, computed, signal } from "@angular/core";
import { isQueuedMutation, MutationResult, StaffAppService, StaffAttendance, StaffToday } from "../../core/staff-app.service";
import { businessDate } from "../../core/business-date";
import { StaffPageStateComponent } from "./staff-page-state.component";

@Component({
  standalone: true,
  imports: [DatePipe, StaffPageStateComponent],
  template: `
    <section class="page attendance-page">
      <header class="page-head attendance-head"><div><h1>Attendance</h1><p>Your shift, attendance timeline, and monthly summary.</p></div><span class="date-chip">{{ today().date | date:'EEE, d MMM' }}</span></header>
      @if (!canUseAttendance()) { <section staffPageState class="notice">You do not have permission to use attendance controls.</section> }
      @if (message()) { <section staffPageState class="notice success">{{ message() }}</section> }
      @if (localError()) { <section staffPageState class="notice">{{ localError() }}</section> }
      @if (staff.error() && !localError()) { <section staffPageState class="notice">{{ staff.error() }}</section> }
      @if (today()) {
        <nav class="attendance-tabs" aria-label="Attendance views">
          @for (item of views; track item.id) { <button type="button" [class.active]="view() === item.id" [attr.aria-pressed]="view() === item.id" (click)="view.set(item.id)"><span class="legacy-binding" aria-hidden="true">{{ item.icon }}</span>{{ item.label }}</button> }
        </nav>
        @if (view() === 'history') { <div class="history-presets attendance-segments shared-range" role="group" aria-label="Attendance reporting range">@for (option of historyRanges; track option.days) { <button type="button" [class.active]="selectedDays() === option.days" [attr.aria-pressed]="selectedDays() === option.days" [disabled]="historyLoading()" (click)="selectHistoryRange(option.days)"><span>{{ option.label }}</span></button> }</div> }

        @if (view() === 'today') {
          <section class="today-card">
            <div class="today-status"><span class="status-dot" [class.active]="!!activeAttendance()" aria-hidden="true"></span><div><p>Today’s Shift</p><span class="field-label">Status</span><span class="current-status" [class.active]="!!activeAttendance()"><i aria-hidden="true"></i>{{ attendanceStatus() }}</span><span class="field-label">Shift name</span><h2>{{ shiftName() }}</h2><small>{{ scheduledShiftLabel() }}</small></div></div>
            <div class="today-metrics" aria-label="Today's shift details">
              <div><span>Clock in</span><strong>{{ activeOrLatestAttendance()?.clockInAt ? (activeOrLatestAttendance()?.clockInAt | date:'shortTime') : '—' }}</strong></div>
              <div class="priority-metric"><span>Expected finish</span><strong>{{ scheduledEndLabel() }}</strong></div>
              <div><span>Worked hours</span><strong>{{ workedLabel() }}</strong></div>
              <div class="secondary-metric"><span>Clock out</span><strong>{{ activeOrLatestAttendance()?.clockOutAt ? (activeOrLatestAttendance()?.clockOutAt | date:'shortTime') : '—' }}</strong></div>
              <div class="secondary-metric"><span>Shift remaining</span><strong>{{ remainingShiftLabel() }}</strong></div>
              @if (overtimeEnabled()) { <div class="overtime-metric"><span>Overtime</span><strong>{{ overtimeWorkedLabel() }}</strong></div> }
            </div>
          </section>
          @if (canUseAttendance()) {
            <div class="attendance-actions" aria-label="Attendance actions">
              @if (!activeAttendance()) { <button type="button" [disabled]="!!pendingAction()" (click)="clockIn()">{{ pendingAction() === 'clock-in' ? 'Clocking in…' : 'Clock in' }}</button> }
              @if (activeAttendance()) {
                <button type="button" [disabled]="!!pendingAction()" (click)="clockOut()">{{ pendingAction() === 'clock-out' ? 'Clocking out…' : 'Clock out' }}</button>
                @if (!isOnBreak()) { <button type="button" [disabled]="!!pendingAction()" (click)="startBreak()">{{ pendingAction() === 'start-break' ? 'Starting break…' : 'Start break' }}</button> }
                @if (isOnBreak()) { <button type="button" [disabled]="!!pendingAction()" (click)="endBreak()">{{ pendingAction() === 'end-break' ? 'Ending break…' : 'End break' }}</button> }
              }
            </div>
          }
          @if (verificationStatus()) { <section staffPageState class="notice verification-status" role="status" aria-live="polite">{{ verificationStatus() }}</section> }
          @if (verificationEvidence()) { <section staffPageState class="notice success verification-result" role="status">{{ verificationEvidence() }}</section> }
          @if (verificationPolicy()) { <section class="privacy-disclosure"><strong>Secure attendance privacy</strong><p>Clock actions capture your precise location, accuracy, device trust, biometric verification result, mock-location signal and optional device integrity verdict. Evidence is retained under your salon's attendance policy. Break actions do not run this secure flow.</p></section> }
          @if (overtimeEnabled()) {
            <section class="overtime-panel" aria-labelledby="overtime-title">
              <div class="overtime-heading"><div><p class="eyebrow">Owner enabled</p><h2 id="overtime-title">Adjusted Shift Overtime</h2></div><span>Policy active</span></div>
              <div class="overtime-rule"><span aria-hidden="true">i</span><p><strong>Overtime begins after the adjusted expected shift end.</strong> A delayed clock-in moves the expected shift end by the same policy-controlled duration.</p></div>
               <div class="overtime-grid"><article><span>Expected shift end</span><strong>{{ scheduledEndLabel() }}</strong><small>Frozen when you clocked in</small></article><article><span>Actual clock out</span><strong>{{ activeOrLatestAttendance()?.clockOutAt ? (activeOrLatestAttendance()?.clockOutAt | date:'shortTime') : '—' }}</strong><small>Recorded attendance time</small></article><article><span>Overtime started</span><strong>{{ overtimeStartLabel() }}</strong><small>After adjusted shift end</small></article><article><span>Overtime worked</span><strong>{{ overtimeWorkedLabel() }}</strong><small>Recorded by the attendance policy</small></article><article><span>Overtime overview</span><strong>{{ overtimeWorkedLabel() }}</strong><small>Current recorded value</small></article></div>
            </section>
          }
          <button class="monthly-glance" type="button" (click)="view.set('monthly')" aria-label="Open this month summary"><span class="glance-title"><small>Monthly overview</small><b>{{ currentMonthLabel() }}</b></span><span><strong>{{ monthlyStats().presentDays }}</strong><small>Present</small></span><span><strong>{{ formatMinutes(monthlyStats().workedMinutes) }}</strong><small>Worked</small></span>@if (overtimeEnabled()) { <span><strong>{{ monthlyOvertimeLabel() }}</strong><small>Overtime</small></span> }<i aria-hidden="true">›</i></button>
        }

        @if (view() === 'history') {
          <section class="history-panel" [attr.aria-busy]="historyLoading()">
            <div class="section-heading"><div><p class="eyebrow">Attendance history</p><h2>{{ activeRangeLabel() }}</h2></div><span>{{ historyLoading() ? 'Loading…' : attendance().length + ' records' }}</span></div>
            <div class="attendance-cards">@for (row of attendance(); track row.id) { <details class="attendance-record"><summary><div class="record-heading"><div class="history-date"><strong>{{ row.businessDate | date:'d' }}</strong><span>{{ row.businessDate | date:'MMM' }}</span></div><div><strong>{{ row.businessDate | date:'EEEE' }}</strong><small>{{ row.businessDate }}</small></div><span class="badge">{{ row.status }}</span><i aria-hidden="true"></i></div><div class="record-metrics"><span><b>Clock in</b>{{ row.clockInAt ? (row.clockInAt | date:'shortTime') : '—' }}</span><span><b>Clock out</b>{{ row.clockOutAt ? (row.clockOutAt | date:'shortTime') : 'Open' }}</span><span><b>Worked</b>{{ formatMinutes(row.totalWorkedMinutes) }}</span><span><b>Shift</b>{{ row.scheduledShiftMinutes === null ? 'Not captured' : formatMinutes(row.scheduledShiftMinutes) }}</span><span><b>Break time</b>{{ formatMinutes(row.totalBreakMinutes) }}</span>@if (overtimeEnabled()) { <span class="overtime"><b>Overtime</b>{{ formatMinutes(row.overtimeMinutes) }}</span> }</div></summary><div class="record-details"><div><span>Attendance source</span><strong>{{ row.source || 'staff-app' }}</strong></div><div><span>Gross recorded time</span><strong>{{ formatMinutes(row.grossMinutes) }}</strong></div>@if (overtimeEnabled()) { <div><span>Overtime calculation</span><strong>{{ row.overtimeCalculationStatus || 'Unavailable' }}</strong></div><div><span>Policy version</span><strong>{{ row.overtimePolicyVersion || 'Unavailable' }}</strong></div>@if (row.overtimeReviewReason) { <div class="wide"><span>Review note</span><strong>{{ row.overtimeReviewReason }}</strong></div> } }</div></details> } @empty { @if (!historyLoading()) { <div class="attendance-empty"><span aria-hidden="true">—</span><strong>No attendance yet</strong><p>After your first clock-out, this timeline will show shift times, breaks, worked hours, and status.</p></div> } }</div>
          </section>
        }

        @if (view() === 'monthly') {
          <section class="monthly-panel">
            <div class="section-heading"><div><p class="eyebrow">Monthly attendance</p><h2>{{ currentMonthLabel() }}</h2></div><span>Recorded attendance</span></div>
            @if (monthlyStats().recordedRows) { <div class="monthly-stats"><article class="present"><span>Present days</span><strong>{{ monthlyStats().presentDays }}</strong><small>Clock-in recorded</small></article><article class="late"><span>Late days</span><strong>{{ monthlyStats().lateDays }}</strong><small>Marked late</small></article><article class="absent"><span>Absent days</span><strong>{{ monthlyStats().absentDays }}</strong><small>Recorded absent</small></article><article><span>Leave days</span><strong>{{ monthlyStats().leaveDays }}</strong><small>Approved or recorded</small></article><article><span>Half days</span><strong>{{ monthlyStats().halfDays }}</strong><small>Half-day status</small></article><article><span>Average working hours</span><strong>{{ formatMinutes(monthlyStats().averageMinutes) }}</strong><small>Per present day</small></article><article><span>Total worked hours</span><strong>{{ formatMinutes(monthlyStats().workedMinutes) }}</strong><small>Current month</small></article>@if (overtimeEnabled()) { <article class="overtime"><span>Overtime</span><strong>{{ monthlyOvertimeLabel() }}</strong><small>Current recorded value</small></article> }</div> } @else { <div class="monthly-stats placeholder-stats" aria-label="Monthly statistics not yet available"><article class="present"><span>Present</span><strong>—</strong><small>After first clock-out</small></article><article class="late"><span>Late</span><strong>—</strong><small>After first attendance</small></article><article><span>Leave</span><strong>—</strong><small>When leave is recorded</small></article><article><span>Worked hours</span><strong>—</strong><small>After first clock-out</small></article>@if (overtimeEnabled()) { <article class="overtime"><span>Overtime</span><strong>{{ monthlyOvertimeLabel() }}</strong><small>Current recorded value</small></article> }</div><div class="attendance-empty"><span aria-hidden="true">—</span><strong>No monthly attendance yet</strong><p>After your first attendance record, this overview will show present, late, leave, and worked-hour totals.</p></div> }
          </section>
        }
      }
    </section>
  `,
  styleUrls: ["./staff-app.styles.css"],
  styles: [`
    :host{display:block}.attendance-page{max-width:1180px;gap:14px}.attendance-head{display:flex;align-items:end;justify-content:space-between}.date-chip{padding:7px 11px;border:1px solid var(--staff-border);border-radius:999px;color:var(--staff-text-secondary);background:var(--staff-surface-secondary);font-size:.72rem;font-weight:750;white-space:nowrap}.attendance-tabs{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:5px;padding:5px;border:1px solid var(--staff-border);border-radius:18px;background:var(--staff-surface)}.attendance-tabs button{display:flex;min-width:0;min-height:46px;align-items:center;justify-content:center;gap:7px;border:0;border-radius:13px;padding:7px 10px;color:var(--staff-text-secondary);background:transparent;font-size:.76rem;font-weight:750;cursor:pointer}.attendance-tabs button>span{display:grid;width:24px;height:24px;place-items:center;border-radius:8px;background:var(--staff-surface-secondary);font-size:.58rem}.attendance-tabs button.active{color:var(--staff-primary-hover);background:var(--staff-primary-light)}.today-card,.history-panel,.analytics-panel,.monthly-panel{overflow:hidden;border:1px solid var(--staff-border);border-radius:24px;background:var(--staff-surface);box-shadow:var(--staff-shadow)}.today-card{display:grid;grid-template-columns:minmax(210px,.8fr) minmax(360px,1.35fr) auto;align-items:center;gap:22px;padding:22px}.today-status{display:flex;align-items:center;gap:13px}.status-dot{width:12px;height:12px;flex:0 0 12px;border-radius:50%;background:var(--staff-text-tertiary);box-shadow:0 0 0 6px color-mix(in srgb,var(--staff-text-tertiary) 12%,transparent)}.status-dot.active{background:var(--staff-success);box-shadow:0 0 0 6px color-mix(in srgb,var(--staff-success) 13%,transparent)}.today-status p,.today-status h2,.today-status small{margin:0}.today-status p{color:var(--staff-text-secondary);font-size:.68rem;font-weight:800;letter-spacing:.06em;text-transform:uppercase}.today-status h2{margin-top:4px;color:var(--staff-text);font-size:1.35rem;text-transform:capitalize}.today-status small{display:block;margin-top:3px;color:var(--staff-text-secondary);font-size:.68rem}.today-metrics{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));overflow:hidden;border:1px solid var(--staff-border);border-radius:16px}.today-metrics>div{min-width:0;padding:12px;border-right:1px solid var(--staff-border)}.today-metrics>div:last-child{border-right:0}.today-metrics span,.analytics-grid span{display:block;color:var(--staff-text-secondary);font-size:.62rem;font-weight:750;text-transform:uppercase}.today-metrics strong{display:block;overflow:hidden;margin-top:5px;color:var(--staff-text);font-size:.94rem;text-overflow:ellipsis;white-space:nowrap}.attendance-actions{display:flex;gap:7px}.attendance-actions button{min-height:44px;border-radius:999px;padding:9px 16px;font:inherit;font-size:.76rem;font-weight:800;cursor:pointer}.attendance-actions button:disabled{cursor:wait;opacity:.55}.primary-action{border:0;color:var(--staff-primary-contrast);background:var(--staff-primary)}.primary-action.danger{color:var(--staff-danger);background:var(--staff-danger-light)}.secondary-action{border:1px solid var(--staff-border);color:var(--staff-text);background:var(--staff-surface-secondary)}.section-heading{display:flex;align-items:end;justify-content:space-between;gap:12px;padding:18px 20px;border-bottom:1px solid var(--staff-border)}.section-heading .eyebrow{margin-bottom:3px}.section-heading h2{margin:0;color:var(--staff-text);font-size:1.05rem}.section-heading>span{color:var(--staff-text-secondary);font-size:.7rem;font-weight:700}.history-presets{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:4px;margin:12px 16px;padding:4px;border:1px solid var(--staff-border);border-radius:14px;background:var(--staff-surface-secondary)}.history-presets button{min-height:38px;border:0;border-radius:10px;color:var(--staff-text-secondary);background:transparent;font-size:.7rem;font-weight:750}.history-presets button.active{color:var(--staff-primary-hover);background:var(--staff-primary-light)}.attendance-list{padding:0 18px 8px}.attendance-list article{display:grid;grid-template-columns:46px minmax(0,1fr);gap:12px;padding:12px 0;border-top:1px solid var(--staff-border)}.history-date{display:grid;align-content:center;justify-items:center;border-radius:12px;background:var(--staff-surface-secondary)}.history-date strong{color:var(--staff-text);font-size:1rem}.history-date span{color:var(--staff-text-secondary);font-size:.58rem;font-weight:750;text-transform:uppercase}.history-main{min-width:0}.history-main>div{display:flex;align-items:center;justify-content:space-between;gap:8px}.history-main strong{color:var(--staff-text);font-size:.78rem}.history-main small{display:block;margin-top:3px;color:var(--staff-text-secondary);font-size:.64rem;line-height:1.25}.history-main .badge{flex:0 0 auto;padding:4px 7px;font-size:.56rem}.analytics-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:1px;background:var(--staff-border)}.analytics-grid article{padding:20px;background:var(--staff-surface)}.analytics-grid strong{display:block;margin-top:8px;color:var(--staff-text);font-size:1.4rem}.analytics-grid small{display:block;margin-top:4px;color:var(--staff-text-secondary);font-size:.65rem}.monthly-list{padding:4px 20px 10px}.monthly-list article{display:flex;align-items:center;justify-content:space-between;gap:16px;padding:14px 0;border-bottom:1px solid var(--staff-border)}.monthly-list article:last-child{border-bottom:0}.monthly-list strong,.monthly-list span{display:block;color:var(--staff-text);font-size:.78rem}.monthly-list small{display:block;margin-top:3px;color:var(--staff-text-secondary);font-size:.64rem}.monthly-list article>div:last-child{text-align:right}.empty{padding:24px;color:var(--staff-text-secondary);text-align:center}
    .today-card{grid-template-columns:minmax(230px,.9fr) minmax(0,1.5fr)}.status-dot{align-self:flex-start;margin-top:5px;background:var(--staff-text-secondary);box-shadow:0 0 0 6px color-mix(in srgb,var(--staff-text-secondary) 12%,transparent)}.today-status .field-label{display:block;margin-top:9px;color:var(--staff-text-secondary);font-size:.58rem;font-weight:800;letter-spacing:.05em;text-transform:uppercase}.today-status small{display:flex;flex-wrap:wrap;gap:5px;margin-top:6px}.today-status small b{color:var(--staff-text-secondary);font-size:.58rem;letter-spacing:.04em;text-transform:uppercase}.current-status{display:inline-flex;align-items:center;gap:6px;margin-top:10px;border-radius:999px;padding:5px 8px;color:var(--staff-primary-hover);background:var(--staff-primary-light);font-size:.6rem;font-weight:800;text-transform:capitalize}.current-status i{width:6px;height:6px;border-radius:50%;background:var(--staff-text-secondary)}.current-status.active i{background:var(--staff-success)}
    .verification-status{font-weight:750}.privacy-disclosure{border:1px solid var(--staff-border);border-radius:16px;padding:12px 14px;background:var(--staff-surface-secondary)}.privacy-disclosure strong{color:var(--staff-text);font-size:.72rem}.privacy-disclosure p{margin:4px 0 0;color:var(--staff-text-secondary);font-size:.64rem;line-height:1.5}
    .overtime-panel{overflow:hidden;border:1px solid var(--staff-border);border-radius:20px;background:var(--staff-surface);box-shadow:var(--staff-shadow)}.overtime-heading{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:13px 16px;border-bottom:1px solid var(--staff-border)}.overtime-heading .eyebrow{margin-bottom:2px;font-size:.55rem}.overtime-heading h2{margin:0;color:var(--staff-text);font-size:.92rem}.overtime-heading>span{border-radius:999px;padding:5px 8px;color:var(--staff-primary-hover);background:var(--staff-primary-light);font-size:.58rem;font-weight:800}.overtime-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:1px;background:var(--staff-border)}.overtime-grid article{padding:14px 16px;background:var(--staff-surface)}.overtime-grid span{display:block;color:var(--staff-text-secondary);font-size:.58rem;font-weight:800;letter-spacing:.04em;text-transform:uppercase}.overtime-grid strong{display:block;margin-top:6px;color:var(--staff-text);font-size:1.12rem}.overtime-grid small{display:block;margin-top:3px;color:var(--staff-text-secondary);font-size:.58rem}.shared-range{margin:0}
    .overtime-rule{display:flex;align-items:flex-start;gap:9px;padding:10px 16px;border-bottom:1px solid var(--staff-border);background:var(--staff-primary-light)}.overtime-rule>span{display:grid;width:20px;height:20px;flex:0 0 20px;place-items:center;border-radius:50%;color:var(--staff-on-primary);background:var(--staff-primary);font-size:.62rem;font-weight:900}.overtime-rule p{margin:0;color:var(--staff-text-secondary);font-size:.65rem;line-height:1.4}.overtime-rule strong{color:var(--staff-text)}.overtime-grid{grid-template-columns:repeat(5,minmax(0,1fr))}
    .attendance-timeline{display:grid;gap:8px;padding:10px}.timeline-record{overflow:hidden;border:1px solid var(--staff-border);border-radius:16px;background:var(--staff-surface-secondary)}.timeline-record>header{display:grid;grid-template-columns:42px minmax(0,1fr) auto;align-items:center;gap:10px;padding:10px 12px;border-bottom:1px solid var(--staff-border)}.timeline-record>header>div:nth-child(2)>strong{display:block;color:var(--staff-text);font-size:.72rem}.timeline-record>header>div:nth-child(2)>small{display:block;margin-top:2px;color:var(--staff-text-secondary);font-size:.56rem}.event-timeline{position:relative;display:grid;grid-auto-flow:column;grid-auto-columns:minmax(0,1fr);gap:6px;padding:13px 12px 10px}.event-timeline::before{position:absolute;top:20px;right:12%;left:12%;height:1px;background:var(--staff-border);content:""}.timeline-event{position:relative;z-index:1;display:grid;justify-items:center;min-width:0;text-align:center}.timeline-event i{width:14px;height:14px;border:3px solid var(--staff-surface-secondary);border-radius:50%;background:var(--staff-text-secondary);box-shadow:0 0 0 1px var(--staff-border)}.timeline-event.complete i{background:var(--staff-success)}.timeline-event.muted i{background:var(--staff-border-accent)}.timeline-event span{margin-top:7px;color:var(--staff-text-secondary);font-size:.56rem;font-weight:750}.timeline-event strong{margin-top:2px;color:var(--staff-text);font-size:.68rem}.timeline-event small{margin-top:2px;color:var(--staff-text-secondary);font-size:.52rem}.timeline-record>footer{display:flex;flex-wrap:wrap;gap:5px;padding:8px 12px;border-top:1px solid var(--staff-border)}.timeline-record>footer span{display:inline-flex;align-items:center;gap:5px;border-radius:999px;padding:4px 7px;color:var(--staff-text);background:var(--staff-surface);font-size:.58rem}.timeline-record>footer b{color:var(--staff-text-secondary);font-size:.53rem;text-transform:uppercase}.timeline-record>footer .overtime{color:var(--staff-primary-hover);background:var(--staff-primary-light)}
    .monthly-stats{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:1px;background:var(--staff-border)}.monthly-stats article{position:relative;min-width:0;padding:16px;background:var(--staff-surface)}.monthly-stats article::before{position:absolute;top:0;right:0;left:0;height:2px;background:var(--staff-border-accent);content:""}.monthly-stats article.present::before{background:var(--staff-success)}.monthly-stats article.late::before{background:var(--staff-warning)}.monthly-stats article.absent::before{background:var(--staff-danger)}.monthly-stats article.overtime::before{background:var(--staff-primary)}.monthly-stats span{display:block;color:var(--staff-text-secondary);font-size:.58rem;font-weight:800;letter-spacing:.045em;text-transform:uppercase}.monthly-stats strong{display:block;margin-top:7px;color:var(--staff-text);font-size:1.25rem;letter-spacing:-.03em}.monthly-stats small{display:block;margin-top:3px;color:var(--staff-text-secondary);font-size:.58rem}
    .attendance-cards{display:grid;gap:8px;padding:10px}.attendance-record{overflow:hidden;border:1px solid var(--staff-border);border-radius:17px;background:var(--staff-surface-secondary)}.attendance-record>summary{list-style:none;cursor:pointer}.attendance-record>summary::-webkit-details-marker{display:none}.record-heading{display:grid;grid-template-columns:42px minmax(0,1fr) auto 18px;align-items:center;gap:10px;padding:10px 12px}.record-heading>div:nth-child(2)>strong{display:block;color:var(--staff-text);font-size:.74rem}.record-heading>div:nth-child(2)>small{display:block;margin-top:2px;color:var(--staff-text-secondary);font-size:.56rem}.record-heading>i{width:8px;height:8px;border-right:2px solid var(--staff-text-secondary);border-bottom:2px solid var(--staff-text-secondary);transform:rotate(45deg) translateY(-2px);transition:transform var(--staff-motion-fast) var(--staff-motion-ease)}.attendance-record[open] .record-heading>i{transform:rotate(225deg) translate(-2px,-2px)}.record-metrics{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));border-top:1px solid var(--staff-border)}.record-metrics>span{min-width:0;padding:9px 10px;border-right:1px solid var(--staff-border);color:var(--staff-text);font-size:.66rem;font-weight:750}.record-metrics>span:last-child{border-right:0}.record-metrics b{display:block;margin-bottom:4px;color:var(--staff-text-secondary);font-size:.52rem;letter-spacing:.04em;text-transform:uppercase}.record-metrics .overtime{color:var(--staff-primary-hover);background:var(--staff-primary-light)}.record-details{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:1px;padding:1px;border-top:1px solid var(--staff-border);background:var(--staff-border)}.record-details>div{min-width:0;padding:11px 12px;background:var(--staff-surface)}.record-details .wide{grid-column:1/-1}.record-details span{display:block;color:var(--staff-text-secondary);font-size:.54rem;font-weight:800;text-transform:uppercase}.record-details strong{display:block;margin-top:4px;overflow-wrap:anywhere;color:var(--staff-text);font-size:.68rem;text-transform:capitalize}
    .attendance-segments{position:relative;gap:3px;padding:3px;border-radius:15px;background:var(--staff-surface)}.attendance-segments button{position:relative;min-width:0;min-height:40px;overflow:hidden;border:0;border-radius:12px;padding:6px 8px;color:var(--staff-text-secondary);background:transparent;cursor:pointer;font:inherit;font-size:.68rem;font-weight:800;transition:color var(--staff-motion-fast) var(--staff-motion-ease),background var(--staff-motion-fast) var(--staff-motion-ease),transform var(--staff-motion-fast) var(--staff-motion-ease)}.attendance-segments button:hover:not(:disabled){color:var(--staff-text);background:var(--staff-surface-secondary)}.attendance-segments button.active{color:var(--staff-primary-hover);background:var(--staff-primary-light);box-shadow:inset 0 0 0 1px color-mix(in srgb,var(--staff-primary) 28%,transparent)}.attendance-segments button.active::after{position:absolute;right:24%;bottom:2px;left:24%;height:2px;border-radius:999px;background:var(--staff-primary);content:""}.attendance-segments button:active:not(:disabled){transform:scale(.98)}.attendance-segments button:disabled{cursor:wait;opacity:.55}.attendance-segments button span{position:relative;z-index:1;white-space:nowrap}
    .attendance-tabs{grid-template-columns:repeat(3,minmax(0,1fr))}.today-metrics{grid-template-columns:repeat(auto-fit,minmax(104px,1fr))}.today-metrics>div{border-bottom:0}.today-metrics>div small{display:block;margin-top:3px;color:var(--staff-text-secondary);font-size:.52rem;line-height:1.2}.today-metrics .overtime-metric{background:var(--staff-primary-light)}.monthly-glance{display:grid;grid-template-columns:minmax(150px,1.4fr) repeat(3,minmax(70px,.6fr)) 18px;align-items:center;gap:10px;width:100%;min-height:66px;border:1px solid var(--staff-border);border-radius:18px;padding:10px 14px;color:var(--staff-text);background:var(--staff-surface);box-shadow:var(--staff-shadow);cursor:pointer;text-align:left}.monthly-glance>span{min-width:0}.monthly-glance b,.monthly-glance strong,.monthly-glance small{display:block}.monthly-glance b{font-size:.78rem}.monthly-glance strong{font-size:.88rem}.monthly-glance small{margin-top:2px;color:var(--staff-text-secondary);font-size:.56rem}.monthly-glance i{color:var(--staff-primary-hover);font-size:1.35rem;font-style:normal}.attendance-empty{display:grid;justify-items:center;gap:5px;padding:28px 16px;color:var(--staff-text-secondary);text-align:center}.attendance-empty>span{display:grid;width:36px;height:36px;place-items:center;border-radius:12px;color:var(--staff-primary-hover);background:var(--staff-primary-light);font-size:1rem}.attendance-empty strong{color:var(--staff-text);font-size:.8rem}.attendance-empty p{max-width:300px;margin:0;font-size:.64rem;line-height:1.4}
    .attendance-record>summary:focus-visible,.monthly-glance:focus-visible,.attendance-tabs button:focus-visible,.attendance-segments button:focus-visible{outline:3px solid var(--staff-input-focus-ring);outline-offset:2px}
    .legacy-binding{display:none!important}.attendance-head>div{min-width:0}.attendance-head h1{letter-spacing:-.035em}.attendance-head p{max-width:46ch;line-height:1.45}.attendance-tabs button{min-height:48px;letter-spacing:.01em;transition:color var(--staff-motion-fast) var(--staff-motion-ease),background var(--staff-motion-fast) var(--staff-motion-ease),transform var(--staff-motion-fast) var(--staff-motion-ease)}.attendance-tabs button:hover{color:var(--staff-text);background:var(--staff-surface-secondary)}.attendance-tabs button:active{transform:scale(.98)}.attendance-tabs button.active:hover{color:var(--staff-primary-hover);background:var(--staff-primary-light)}.today-card{align-items:stretch}.today-status>div{min-width:0}.today-status h2{line-height:1.15}.today-status small{align-items:baseline;line-height:1.4}.current-status{min-height:28px}.today-metrics>div{display:flex;min-height:88px;flex-direction:column;justify-content:center}.today-metrics strong{overflow:visible;line-height:1.2;text-overflow:clip;white-space:normal}.today-metrics>div:nth-child(5) strong{max-width:22ch;font-size:.76rem}.monthly-glance{position:relative;transition:border-color var(--staff-motion-fast) var(--staff-motion-ease),background var(--staff-motion-fast) var(--staff-motion-ease),transform var(--staff-motion-fast) var(--staff-motion-ease)}.monthly-glance:hover{border-color:var(--staff-border-accent);background:var(--staff-surface-secondary)}.monthly-glance:active{transform:scale(.99)}.placeholder-stats article{min-height:104px}.placeholder-stats strong{color:var(--staff-text-secondary)}.attendance-empty{padding:32px 18px}.attendance-empty strong{max-width:38ch;font-size:.86rem;line-height:1.35}.attendance-empty p{max-width:48ch;font-size:.68rem;line-height:1.55}.attendance-segments button{min-height:44px}
    .today-card{gap:18px;padding:20px;background:linear-gradient(135deg,var(--staff-surface),color-mix(in srgb,var(--staff-surface-secondary) 58%,var(--staff-surface)))}.today-status{align-items:flex-start}.today-status p{margin-bottom:12px}.today-status .field-label{margin-top:0}.today-status .current-status{margin:5px 0 14px;padding:6px 9px}.today-status h2{margin-top:5px;font-size:1.28rem}.today-status small{margin-top:5px}.today-metrics{align-self:stretch;background:color-mix(in srgb,var(--staff-surface-secondary) 72%,transparent)}.today-metrics>div{min-height:76px;padding:11px 13px;background:var(--staff-surface)}.today-metrics span{letter-spacing:.045em}.today-metrics strong{font-size:1rem;letter-spacing:-.02em}.today-metrics .priority-metric{background:color-mix(in srgb,var(--staff-primary-light) 64%,var(--staff-surface))}.today-metrics .secondary-metric{min-height:68px}.today-metrics .secondary-metric strong{color:var(--staff-text-secondary);font-size:.82rem}.today-metrics .overtime-metric strong{color:var(--staff-primary-hover)}.monthly-glance{grid-template-columns:minmax(170px,1.45fr) repeat(3,minmax(68px,.55fr)) 18px;min-height:72px;overflow:hidden;border-color:color-mix(in srgb,var(--staff-primary) 20%,var(--staff-border));box-shadow:0 10px 28px color-mix(in srgb,var(--staff-text) 6%,transparent)}.monthly-glance::before{position:absolute;top:0;bottom:0;left:0;width:3px;background:var(--staff-primary);content:""}.monthly-glance>span:not(.glance-title){padding-left:12px;border-left:1px solid var(--staff-border)}.monthly-glance .glance-title{padding-left:3px}.monthly-glance .glance-title small{margin:0 0 4px;color:var(--staff-primary-hover);font-weight:800;letter-spacing:.055em;text-transform:uppercase}.monthly-glance .glance-title b{font-size:.84rem;letter-spacing:-.015em}.monthly-glance i{display:grid;width:44px;height:44px;place-items:center;justify-self:end;border-radius:50%;background:var(--staff-primary-light)}.attendance-empty{gap:7px}.attendance-empty>span{width:40px;height:40px}.attendance-empty strong{font-size:.88rem}.attendance-empty p{color:var(--staff-text-secondary)}
    @media(max-width:850px){.today-card{grid-template-columns:1fr}.today-metrics{order:2}.attendance-actions{order:3}.analytics-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}
    @media(max-width:700px){.attendance-page{gap:10px;padding-inline:12px}.attendance-head{min-height:0;align-items:center;gap:12px}.attendance-head h1{font-size:1.5rem;line-height:1}.attendance-head p:not(.eyebrow){margin-top:5px;font-size:.7rem}.date-chip{padding:7px 10px;font-size:.64rem}.attendance-tabs{gap:2px;padding:3px;border-radius:15px}.attendance-tabs button{min-height:44px;gap:2px;padding:6px 4px;font-size:.68rem}.today-card{gap:14px;padding:15px;border-radius:18px}.today-status{gap:11px}.today-status h2{font-size:1.16rem}.today-metrics{grid-template-columns:repeat(3,minmax(0,1fr));border-radius:13px}.today-metrics>div{min-height:82px;padding:10px 8px}.today-metrics span{font-size:.56rem}.today-metrics strong{font-size:.76rem}.today-metrics>div:nth-child(5) strong{font-size:.68rem}.attendance-actions{display:grid;grid-template-columns:repeat(2,minmax(0,1fr))}.attendance-actions button{width:100%;min-height:44px;padding:8px 10px}.attendance-actions button:only-child{grid-column:1/-1}.history-panel,.analytics-panel,.monthly-panel{border-radius:18px}.section-heading{align-items:center;padding:13px 14px}.section-heading h2{font-size:.9rem}.section-heading>span{font-size:.62rem}.history-presets{margin:8px 10px}.history-presets button{min-height:44px;padding:6px 2px;font-size:.63rem}.attendance-list{padding:0 12px 5px}.attendance-list article{grid-template-columns:40px minmax(0,1fr);gap:9px;padding:9px 0}.history-main strong{font-size:.7rem}.history-main small{font-size:.59rem}.analytics-grid article{padding:14px}.analytics-grid strong{font-size:1.08rem}.monthly-list{padding-inline:14px}.monthly-list article{padding:11px 0}.attendance-empty{padding:27px 16px}.attendance-empty p{font-size:.7rem}}
    @media(max-width:700px){.overtime-panel{border-radius:18px}.overtime-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.overtime-grid article{padding:11px 12px}.attendance-timeline{gap:6px;padding:7px}.timeline-record{border-radius:14px}.timeline-record>header{padding:8px 9px}.event-timeline{gap:2px;padding:11px 7px 8px}.event-timeline::before{top:18px}.timeline-event span{font-size:.5rem}.timeline-event strong{font-size:.62rem}.timeline-record>footer{padding:7px 9px}.monthly-stats{grid-template-columns:repeat(2,minmax(0,1fr))}.monthly-stats article{padding:12px}.monthly-stats strong{font-size:1.05rem}.attendance-cards{gap:6px;padding:7px}.attendance-record{border-radius:14px}.record-heading{padding:8px 9px}.record-metrics{grid-template-columns:repeat(3,minmax(0,1fr))}.record-metrics>span{padding:8px;border-bottom:1px solid var(--staff-border)}.record-metrics>span:nth-child(3n){border-right:0}.record-metrics>span:nth-last-child(-n+3){border-bottom:0}.record-details{grid-template-columns:repeat(2,minmax(0,1fr))}.record-details>div{padding:9px}.today-metrics{grid-template-columns:repeat(3,minmax(0,1fr))}.monthly-glance{grid-template-columns:minmax(118px,1.2fr) repeat(3,minmax(48px,.55fr)) 44px;gap:6px;padding:9px 10px 9px 13px}.monthly-glance>span:not(.glance-title){padding-left:7px}.monthly-glance b{font-size:.7rem}.monthly-glance strong{font-size:.74rem}.monthly-glance small{font-size:.52rem}}
    @media(max-width:380px){.attendance-page{padding-inline:10px}.attendance-head{align-items:flex-start}.attendance-head p:not(.eyebrow){font-size:.66rem}.date-chip{margin-top:1px;padding-inline:8px}.today-card{padding:14px}.today-metrics{grid-template-columns:repeat(2,minmax(0,1fr))}.today-metrics>div{min-height:72px}.today-metrics>div:nth-child(2){border-right:0}.monthly-glance{grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;padding:12px 13px}.monthly-glance>span:first-child{grid-column:1/-1;padding-right:48px}.monthly-glance>span:not(.glance-title){padding-left:0;border-left:0}.monthly-glance i{position:absolute;top:12px;right:11px}.monthly-glance strong{font-size:.8rem}.monthly-glance small{font-size:.55rem}.record-heading{grid-template-columns:40px minmax(0,1fr) auto 14px;gap:7px}.record-metrics{grid-template-columns:repeat(2,minmax(0,1fr))}.record-metrics>span:nth-child(3n){border-right:1px solid var(--staff-border)}.record-metrics>span:nth-child(2n){border-right:0}.record-metrics>span:nth-last-child(-n+3){border-bottom:1px solid var(--staff-border)}.record-metrics>span:nth-last-child(-n+2){border-bottom:0}}
    @media(max-width:360px){.attendance-head p:not(.eyebrow){display:none}.attendance-tabs button{font-size:.64rem}.today-metrics{grid-template-columns:repeat(2,minmax(0,1fr))}.today-metrics>div:nth-child(2){border-right:0}.today-metrics>div:nth-child(-n+2){border-bottom:1px solid var(--staff-border)}}
    @media(max-width:700px){.attendance-page{padding-inline:14px}.attendance-head{display:grid;grid-template-columns:minmax(0,1fr) auto;align-items:start}.attendance-tabs{grid-template-columns:repeat(3,minmax(0,1fr));position:sticky;top:var(--staff-header-height);z-index:4}.today-card{grid-template-columns:1fr}.today-metrics{grid-template-columns:repeat(2,minmax(0,1fr))}.today-metrics>div{min-height:74px}.attendance-actions{display:grid;grid-template-columns:1fr 1fr;gap:8px}.overtime-grid,.monthly-stats,.record-details{grid-template-columns:1fr 1fr}.record-metrics{grid-template-columns:repeat(2,minmax(0,1fr))}.monthly-glance{grid-template-columns:1fr auto;gap:10px}.monthly-glance>span:first-child{grid-column:1/-1;padding-right:44px}.monthly-glance>span:not(.glance-title){padding-left:0;border-left:0}.monthly-glance i{position:absolute;top:12px;right:12px}.event-timeline{grid-auto-flow:row;grid-auto-columns:auto;grid-template-columns:1fr 1fr}.event-timeline::before{display:none}.timeline-event{justify-items:start;text-align:left}.timeline-event i{position:absolute;left:0;top:2px}.timeline-event span,.timeline-event strong,.timeline-event small{padding-left:24px}}
    @media(max-width:380px){.attendance-page{padding-inline:12px}.attendance-head{grid-template-columns:1fr}.date-chip{justify-self:start}.today-metrics,.overtime-grid,.monthly-stats,.record-details,.record-metrics{grid-template-columns:1fr}.attendance-actions{grid-template-columns:1fr}.attendance-tabs button{font-size:.68rem}.monthly-glance{grid-template-columns:1fr}.monthly-glance>span:first-child{padding-right:40px}}
    @media(prefers-reduced-motion:reduce){.attendance-tabs button,.attendance-segments button,.monthly-glance,.record-heading>i{transition:none}.attendance-tabs button:active,.attendance-segments button:active:not(:disabled),.monthly-glance:active{transform:none}}
  `]
})
export class StaffAttendancePage implements OnInit, OnDestroy {
  readonly today = signal<StaffToday>(this.staff.readStoredData<StaffToday>("today") || { date: businessDate(), schedules: [], attendance: [], activeBreak: null, tasks: [] });
  readonly attendance = signal<StaffAttendance[]>(this.staff.readStoredData<StaffAttendance[]>("attendance-history") || []);
  readonly monthlyAttendance = signal<StaffAttendance[]>([]);
  readonly currentTime = signal(Date.now());
  readonly view = signal<"today" | "history" | "monthly">("today");
  readonly views = [{ id: "today", label: "Today", icon: "TD" }, { id: "history", label: "Timeline", icon: "TL" }, { id: "monthly", label: "Month", icon: "MO" }] as const;
  readonly historyLoading = signal(false);
  readonly selectedDays = signal<30 | 90 | 180 | 365>(30);
  readonly historyRanges = [{ days: 30, label: "30 days", rangeLabel: "Last 30 days" }, { days: 90, label: "3 months", rangeLabel: "Last 3 months" }, { days: 180, label: "6 months", rangeLabel: "Last 6 months" }, { days: 365, label: "12 months", rangeLabel: "Last 12 months" }] as const;
  readonly activeRangeLabel = computed(() => this.historyRanges.find((option) => option.days === this.selectedDays())?.rangeLabel || "Last 30 days");
  readonly overtimeEnabled = computed(() => this.activeOrLatestAttendance()?.overtimeEnabled === true || [...this.attendance(), ...this.monthlyAttendance()].some((row) => row.overtimeEnabled === true));
  readonly monthlyStats = computed(() => {
    const monthKey = String(this.today()?.date || "").slice(0, 7);
    const rows = this.monthlyAttendance().filter((row) => row.businessDate.startsWith(monthKey));
    const hasStatus = (row: StaffAttendance, value: string) => String(row.status || "").toLowerCase().includes(value);
    const presentDays = rows.filter((row) => !!row.clockInAt).length;
    const workedMinutes = rows.reduce((total, row) => total + Number(row.totalWorkedMinutes || 0), 0);
    const overtimeMinutes = rows.reduce((total, row) => total + Number(row.overtimeMinutes || 0), 0);
    return { recordedRows: rows.length, presentDays, lateDays: rows.filter((row) => hasStatus(row, "late")).length, absentDays: rows.filter((row) => hasStatus(row, "absent")).length, leaveDays: rows.filter((row) => hasStatus(row, "leave")).length, halfDays: rows.filter((row) => hasStatus(row, "half")).length, workedMinutes, averageMinutes: presentDays ? Math.round(workedMinutes / presentDays) : 0, overtimeMinutes };
  });
  readonly message = signal("");
  readonly localError = signal("");
  readonly pendingAction = signal<"clock-in" | "clock-out" | "start-break" | "end-break" | null>(null);
  readonly verificationPolicy = signal(false);
  readonly verificationStatus = computed(() => ({ "checking-policy": "Checking branch attendance policy...", "checking-device": "Checking trusted device...", "getting-location": "Capturing precise native location...", "verify-biometric": "Verify with your device biometric...", submitting: "Submitting verified attendance...", "": "" } as const)[this.staff.attendanceVerificationProgress()]);
  readonly verificationEvidence = computed(() => {
    const evidence = this.staff.attendanceVerificationEvidence();
    if (!evidence) return "";
    const values: string[] = [];
    if (typeof evidence.serverDistanceMeters === "number") values.push(`Distance ${Math.round(evidence.serverDistanceMeters)} m`);
    if (typeof evidence.accuracyMeters === "number") values.push(`Accuracy ${Math.round(evidence.accuracyMeters)} m`);
    if (this.staff.attendanceDeviceStatus()) values.push(`Device ${this.staff.attendanceDeviceStatus()}`);
    return values.join(" · ");
  });
  readonly activeAttendance = computed(() => this.today()?.attendance.find((item) => ["clocked_in", "on_break", "break"].includes(String(item.status).toLowerCase())) || this.attendance().find((item) => ["clocked_in", "on_break", "break"].includes(String(item.status).toLowerCase())) || null);
  readonly activeOrLatestAttendance = computed<StaffAttendance | null>(() => this.activeAttendance() || this.today()?.attendance[0] || null);
  readonly todayShift = computed(() => this.today()?.schedules[0] || null);
  private readonly attendanceUpdated = () => {
    if (this.reloadTimer !== null) clearTimeout(this.reloadTimer);
    this.reloadTimer = setTimeout(() => { this.reloadTimer = null; void this.load(true); }, 800);
  };
  private reloadTimer: ReturnType<typeof setTimeout> | null = null;
  private loadGeneration = 0;
  private historyGeneration = 0;
  private minuteTimer: ReturnType<typeof setInterval> | null = null;
  constructor(readonly staff: StaffAppService) {}
  ngOnInit() { window.addEventListener("aura:attendance-updated", this.attendanceUpdated); this.minuteTimer = setInterval(() => this.currentTime.set(Date.now()), 60_000); void this.load(); }
  ngOnDestroy() { window.removeEventListener("aura:attendance-updated", this.attendanceUpdated); if (this.reloadTimer !== null) clearTimeout(this.reloadTimer); if (this.minuteTimer) clearInterval(this.minuteTimer); this.loadGeneration += 1; this.historyGeneration += 1; }
  async load(fresh = false) {
    const generation = ++this.loadGeneration;
    const historyGeneration = ++this.historyGeneration;
    try {
      const date = businessDate();
      const monthStart = `${date.slice(0, 7)}-01`;
      const [today, attendance, monthlyAttendance, policy] = await Promise.all([this.staff.today(undefined, fresh), this.staff.attendanceHistory(this.selectedDays()), this.staff.attendanceHistoryRange(monthStart, date), this.staff.attendanceVerificationPolicy().catch(() => null)]);
      if (generation !== this.loadGeneration) return;
      this.today.set(today);
      this.staff.writeStoredData("today", today);
      if (historyGeneration === this.historyGeneration) {
        this.attendance.set(attendance);
        this.staff.writeStoredData("attendance-history", attendance);
      }
      this.monthlyAttendance.set(monthlyAttendance);
      this.verificationPolicy.set(!!policy && policy.status === "active" && (policy.enforceClockIn || policy.enforceClockOut));
    } catch {
      // Backend error handled by StaffAppService
    }
  }
  canUseAttendance(): boolean { return this.staff.hasAnyPermission(["allow:staff-checkin-checkout", "write:staff"]); }
  attendanceStatus(): string { return this.activeOrLatestAttendance()?.status?.replace(/_/g, " ") || "not clocked in"; }
  shiftName(): string { return this.todayShift()?.shiftType?.replace(/_/g, " ") || "No shift assigned"; }
  scheduledShiftLabel(): string { const shift = this.todayShift(); return shift?.startTime && shift?.endTime ? `${shift.startTime} – ${shift.endTime}` : "Not scheduled"; }
  scheduledEndLabel(): string { const expected = this.expectedEndMs(); return expected === null ? "not set" : new Intl.DateTimeFormat("en-IN", { hour: "numeric", minute: "2-digit", timeZone: "Asia/Kolkata" }).format(new Date(expected)); }
  isOnBreak(): boolean { return !!this.today()?.activeBreak || ["on_break", "break"].includes(String(this.activeAttendance()?.status || "").toLowerCase()); }
  workedLabel(): string { const row = this.activeOrLatestAttendance(); if (!row?.clockInAt) return "-"; if (row.clockOutAt) return this.formatMinutes(row.totalWorkedMinutes); const minutes = Math.max(0, Math.floor((this.currentTime() - new Date(row.clockInAt).getTime()) / 60000) - Number(row.totalBreakMinutes || 0)); return this.formatMinutes(minutes); }
  remainingShiftLabel(): string {
    const attendance = this.activeOrLatestAttendance();
    const endMs = this.expectedEndMs();
    if (endMs === null) return "No shift end set";
    if (!attendance?.clockInAt) return "Not clocked in";
    if (attendance.clockOutAt) return "Shift ended";
    const diff = endMs - this.currentTime();
    if (diff <= 0) return "Overtime";
    return this.formatMinutes(Math.floor(diff / 60000));
  }
  overtimeStartLabel(): string { const expected = this.expectedEndMs(); return expected !== null && this.currentTime() >= expected ? this.scheduledEndLabel() : "—"; }
  overtimeWorkedLabel(): string { const minutes = this.activeOrLatestAttendance()?.overtimeMinutes; return minutes === null || minutes === undefined ? "—" : this.formatMinutes(minutes); }
  monthlyOvertimeLabel(): string { return this.formatMinutes(this.monthlyStats().overtimeMinutes); }
  formatMinutes(value: number | null | undefined): string { const minutes = Math.max(0, Number(value || 0)); return `${Math.floor(minutes / 60)}h ${minutes % 60}m`; }
  currentMonthLabel(): string { const key = String(this.today()?.date || "").slice(0, 7); return /^\d{4}-\d{2}$/.test(key) ? this.monthLabel(key) : "Current month"; }
  private monthLabel(key: string): string { const [year, month] = key.split("-").map(Number); return new Intl.DateTimeFormat("en-IN", { month: "long", year: "numeric", timeZone: "Asia/Kolkata" }).format(new Date(Date.UTC(year, month - 1, 1))); }
  async clockIn() { await this.runAction("clock-in", () => this.staff.clockIn(), "Clock-in saved."); }
  async clockOut() { const active = this.activeAttendance(); const clockInMs = new Date(active?.clockInAt || "").getTime(); if (active && !active.clockOutAt && Number.isFinite(clockInMs) && clockInMs < Date.now() - 36 * 60 * 60 * 1000) { this.localError.set("Your last shift wasn’t clocked out. Ask your owner to close it, then you can clock in fresh."); return; } await this.runAction("clock-out", () => this.staff.clockOut(active?.id), "Clock-out saved."); }
  async startBreak() { await this.runAction("start-break", () => this.staff.startBreak(), "Break started."); }
  async endBreak() { await this.runAction("end-break", () => this.staff.endBreak(), "Break ended."); }
  private async runAction(action: NonNullable<ReturnType<typeof this.pendingAction>>, mutate: () => Promise<MutationResult<unknown>>, completedMessage: string) {
    if (this.pendingAction()) return;
    this.pendingAction.set(action);
    this.message.set("");
    this.localError.set("");
    try {
      const result = await mutate();
      if (isQueuedMutation(result)) {
        this.message.set(`Offline: ${action.replace(/-/g, " ")} queued for sync (${result.queueId}).`);
        return;
      }
      const rec = (result && typeof result === "object" && "data" in result ? (result as { data: StaffAttendance }).data : result) as StaffAttendance;
      if (rec && typeof rec === "object" && rec.id) {
        const curToday = this.today();
        if (curToday) {
          const list = [rec, ...curToday.attendance.filter((a) => a.id !== rec.id)];
          this.today.set({ ...curToday, attendance: list });
        }
      }
      this.message.set(completedMessage);
      await this.load(true);
      window.dispatchEvent(new CustomEvent("aura:attendance-updated"));
    } catch {
      this.localError.set(this.staff.error() || `Unable to ${action.replace(/-/g, " ")}.`);
    } finally {
      this.pendingAction.set(null);
    }
  }
  async selectHistoryRange(days: 30 | 90 | 180 | 365) {
    if (days === this.selectedDays() || this.historyLoading()) return;
    const previousDays = this.selectedDays();
    const previousAttendance = this.attendance();
    this.selectedDays.set(days);
    this.attendance.set([]);
    this.localError.set("");
    this.historyLoading.set(true);
    const generation = ++this.historyGeneration;
    try { const rows = await this.staff.attendanceHistory(days); if (generation === this.historyGeneration) this.attendance.set(rows); }
    catch {
      if (generation !== this.historyGeneration) return;
      this.selectedDays.set(previousDays);
      this.attendance.set(previousAttendance);
      this.localError.set(this.staff.error() || "Unable to load attendance history.");
    }
    finally { if (generation === this.historyGeneration) this.historyLoading.set(false); }
  }
  private expectedEndMs(): number | null {
    const attendance = this.activeOrLatestAttendance();
    if (attendance?.expectedEndAt) { const parsed = Date.parse(attendance.expectedEndAt); if (Number.isFinite(parsed)) return parsed; }
    const shift = this.todayShift();
    const date = String(this.today()?.date || businessDate());
    if (!shift?.startTime || !shift.endTime) return null;
    const start = Date.parse(`${date}T${shift.startTime}:00+05:30`);
    let end = Date.parse(`${date}T${shift.endTime}:00+05:30`);
    if (!Number.isFinite(start) || !Number.isFinite(end)) return null;
    if (end <= start) end += 86_400_000;
    const clockIn = attendance?.clockInAt ? Date.parse(attendance.clockInAt) : Number.NaN;
    return end + (Number.isFinite(clockIn) ? Math.max(0, clockIn - start) : 0);
  }
}
