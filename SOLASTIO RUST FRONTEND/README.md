# Solastio Rust Frontend

This is the old Angular/Ionic frontend copied into a separate migration workspace and pointed at the live Rust API.

- Source reference kept untouched: `D:\Documents\SOLASTIO\old app`
- API base URL: `https://129.159.16.165.sslip.io/api/v1`
- Local dev: `npm start`

Do not add the old Node server back into this folder. Backend changes belong in `NEW SOLASTIO APP/app` only when explicitly requested.
